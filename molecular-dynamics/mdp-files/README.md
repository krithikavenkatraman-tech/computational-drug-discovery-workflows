# MDP Files

MDP (Molecular Dynamics Parameter) files define the simulation settings used by GROMACS.

This folder contains:

- minim.mdp
- nvt.mdp
- npt.mdp
- md.mdp

These files specify simulation parameters such as temperature, pressure, timestep, cutoff values, simulation duration, and output frequency.
